package lectures.week1

object Expressions extends App {
  val codeBlock = {

    val some = 1
    val y = 2
    if (some + y > 1) true else false

    "String to return"
    "meow"
  }

  println(codeBlock)
}
